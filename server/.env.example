# Server Environment Variables
# Copy to .env and fill in values

# Port to run the server on (Railway provides this automatically in production)
PORT=3001

# Environment (development/production)
NODE_ENV=development

# Comma-separated list of allowed origins for CORS
# In development mode, all origins are allowed regardless of this setting
# In production, only these origins can make cross-origin requests
# Example: ALLOWED_ORIGINS=https://paper-magic.vercel.app,https://custom-domain.com
ALLOWED_ORIGINS=http://localhost:5173
